import Link from "next/link";

export default function DemoPage() {
  return <main className="shell"><nav className="nav"><Link className="brand" href="/">leash<span>.</span>agent</Link><div className="navlinks"><Link href="/">Dashboard</Link><Link href="/audit">Audit</Link></div></nav><section className="hero"><div className="hero-copy"><div className="eyebrow">Guided proof</div><h1>Revoke. Call again. Get denied.</h1><p className="lede">The important Leash demo is not a successful request. It is the next request failing after authority changes.</p></div></section><section className="grid"><Step n="01" title="Allow" body="Run an authorised inference.call from the dashboard."/><Step n="02" title="Revoke" body="Use the protected admin endpoint or revoke the ENSv2 role from your wallet."/><Step n="03" title="Deny" body="Repeat the request and inspect NO_ROLES_ON_RESOURCE in the audit stream."/><Step n="04" title="Narrow" body="Grant a smaller role set and prove only the matching capability succeeds."/></section><section className="card"><div className="eyebrow">Admin safety</div><h2>Writes are disabled by default</h2><p className="muted">Role-write routes require both <code>LEASH_ADMIN_ENABLED=true</code> and a server-side <code>PRIVATE_KEY</code>. The browser never receives the key.</p><a href="/"><button>Start with dashboard</button></a></section></main>;
}
function Step({ n, title, body }: { n: string; title: string; body: string }) { return <div className="card"><div className="eyebrow">{n}</div><h2>{title}</h2><p className="muted">{body}</p></div>; }
